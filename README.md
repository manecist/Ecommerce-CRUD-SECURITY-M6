Se suben todos los archivos acorde al proyecto solicitado de logica de CRUD productos con el de usuarios con rol admin y client
Se realiza logica con el usuario no registrado pero que de igual forma a futuro podra comprar sin registrarse
Se corrije el codigo base varias veces debido a los fallos constantes 
Se revisa el pom para que cumpla con lo que busco, las cuales son Spring boot, spring web, devtols, JPA, MySQL, lombok, validation, JWT, security.
Se configura application properties con la base de datos, jpa, thymeleaf, archivos externos, jwt, correo, debug.
Se crea la logica de entidades, repositorios, controladores, servicios, dto, mapper, filtros, configuracion. todo con sub carpetas con la logica de usuario, productos y futuro carrito
Hibernate puede crear por si mismo todo en base de datos, pero muchas veces se ordenan como desean y no es la idea por las consultas en tabla
Se hace la logica de los repository, service y los controller y vistas
